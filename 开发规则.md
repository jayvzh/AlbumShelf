# AlbumShelf・NAS图集馆 开发规则（精简版 v1.0）

> 给 AI 的第一份规则。日常任务先执行本页即可，能省 token 就不必打开完整总则。
> 完整总则：`docs/DEVELOPMENT_RULES.md`（Sprint 开始 / 架构、范围判断时必读；冲突以它为准）。
> 宗旨：先查文档、少扫描、小步改、不超范围。

## 0. 项目一句话
NAS 自托管 Web 图片浏览器。后端 Go1.24+Gin+govips+SQLite；前端 Vue3+TS+Vite+Pinia。按 Sprint 迭代，一次只做一个。

## 1. 文档地图（按任务点开对应文档，禁止整目录通读）
| 任务 | 打开 |
| --- | --- |
| 开新 Sprint / 范围判定 | docs/SPRINT_PLAN.md + docs/SPRINTn_TASK.md + docs/DEVELOPMENT_RULES.md |
| 产品范围 / 非目标 | docs/PRD.md |
| 目录组织 / 模块边界 | docs/PROJECT_STRUCTURE.md |
| 接口、响应格式、错误码 | docs/API.md（唯一契约来源） |
| 建表 / 迁移 | docs/DATA_MODEL.md |
| 排序算法 | docs/SORT_ENGINE.md |
| 双页拼帧算法 | docs/SPREAD_ENGINE.md |
| 缓存 / 图片加载 / 性能 | docs/ARCHITECTURE.md |
| 交互 / 界面 | docs/UI_DESIGN.md |

## 2. 铁律（永久生效，违反即返工）
- 分层只准向下：Handler→Service→Domain→Repository/Filesystem；禁 Handler 直接碰库/碰文件/复杂逻辑；禁 Sorting 碰库。
- 路径安全：一切用户 path 走 `filesystem/path.go`，越界回 `INVALID_PATH`，禁止临时放宽。
- 前端链路唯一：Component→Store→Service→API，禁组件直接 fetch；Store 仅 folder/viewer/spread/settings 四个。
- TS 类型、响应格式、错误码一律以 API.md 为准，禁自造不一致结构。
- 图片加载：默认 Preview、原图仅点击按需；图片 URL 带 v 版本+immutable 缓存头；缩略图懒加载+虚拟渲染，可见数量动态算禁硬编码。
- 双页=帧渲染（ViewerCanvas→SpreadFrame 1~2 图），拼帧逻辑只写 `utils/spread.ts` 纯函数；不做对比工具。
- 排序只在后端完成；图片永不入 DB；迁移只增不改。

## 3. 省 token 的行为约束
- 动手前先查文档与已有实现；检索即取证：不整仓通读、大文件只查片段、同查询不重复、同任务搜索 ≤3 轮。
- 改动最小化、用精确小 diff；不无意义重构；新依赖先说理由；需求含糊先问清，禁止盲目大批生成。
- 注释精简、不批量造测试；回复给结论+必要片段+关键决策理由。
- 改后最小验证，交付摘要=改动范围+验证结果；接口变更双端同步并回写 API.md。

## 4. 常用命令（免读 Makefile）
- 日常自测/热重载：`./scripts/dev.sh start`，改码后 `restart`（前端 http://localhost:5160，后端 :8080；子命令/端口详见 DEVELOPMENT_RULES §1.5）。Docker 部署时浏览器访问端口默认 8160（容器内仍 8080）。
- 功能验证禁止反复 `docker compose up --build`（耗时）；Docker 优化/部署留 Sprint 7 一次完成。
- 构建测试：`make test / lint / build-backend / build-frontend`。

## 5. 交付自检（10 秒）
- 未碰本 Sprint 之外的文件？dev.sh restart 自测通过？build/test 过？与 API.md 一致？
- 完整清单：docs/DEVELOPMENT_RULES.md §10。
